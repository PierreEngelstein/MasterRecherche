// function multiply(m1[3][2], m2[3][2])
//     return ((m1()))
// end
// 

function dot(m1[2][2], m2[2][2])
    return ((m1(1)(1) * m2(1)(1) + m1(1)(2) * m2(2)(1),     m1(1)(1) * m2(1)(2) + m1(1)(2) * m2(2)(2)); 
            (m1(2)(1) * m2(1)(1) + m1(2)(2) * m2(2)(1),     m1(2)(1) * m2(1)(2) + m1(2)(2)*m2(2)(2)));
end
Variables
    // m1[2][2];
    // m2[2][2];
    x in [-1, 1];
    // y in [-1, 1];
    // z in [-1, 1];

Minimize
    // -ln(x);
    // (m1*m2)(1)(1);
    // dot(m1, m2)(1)(1);
    xlog(x);
    // xlog(x) + xlog(y) + xlog(y) + xlog(x+y) + xlog(x+z) + xlog(y+z) + xlog(x+y+z);

Constraints
    // x >= 0;
    // x >= -1;
    // x <= 1;
    // x <= 1;
    // ln(x) >= 0;
    // atanhc(x) <= 0;
end